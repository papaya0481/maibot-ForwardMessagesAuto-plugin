# 插件开发指南

你正在为 MaiBot 编写第三方插件。插件必须放在 plugins/<plugin-name>/ 下，不要修改 MaiBot 主程序代码，除非我明确许可。请使用 maibot-plugin-sdk，入口文件为 plugin.py，元信息文件为 _manifest.json。必须实现 on_load、on_unload、on_config_update 和 create_plugin。优先使用 @Tool、@Command、@HookHandler、@EventHandler、@API、@MessageGateway；不要给新插件使用 @Action。所有用户可见文本优先使用简体中文。请保持改动边界清晰，并给出测试方式。

详细信息以官方 [Vibe Coding 插件开发指南](https://docs.mai-mai.org/plugin/vibe-coding) 为基础约束。Manifest、生命周期、配置管理、各类组件和 SDK API 的详细说明，应继续查阅 [MaiBot 插件开发文档](https://docs.mai-mai.org/plugin/) 下对应的小节；实现细节与本文件冲突时，以官方文档为准。

# 其他事项

- 每完成一处边界清晰、可以独立说明的修改，都应尽量及时创建 commit；代码、文档、配置及其他类型的改动均适用。
- commit 应只包含本次相关改动，并使用简短、明确的说明概括修改内容。
- 使用 coding agent 完成修改时，提交 commit 必须按该 agent 的署名要求，在提交信息末尾附加对应的署名 trailer。
- 插件功能应按职责分散到多个文件中，尽量降低模块之间的耦合；`plugin.py` 只保留插件入口、生命周期、组件声明与依赖装配，避免堆积具体业务实现。
- 优先使用面向对象方式组织代码，将相关状态和行为封装在职责单一的类中，并保持清晰、单向的依赖关系；避免无边界的全局状态、超大类和循环依赖。
- 每项测试都必须通过测试函数注释或文档字符串说明其测试场景、预期行为或所防止的回归，确保失败时能够快速理解测试意图。
- 测试文件必须按被测职责分类；不要持续向单个 ``tests/test_plugin.py`` 堆叠测试。跨类别复用的替身和构造辅助函数应集中在明确的测试支持模块，避免测试模块之间形成隐式耦合。
- 插件版本采用语义化版本号，首个版本为 `0.1.0`。
- 配置版本独立于插件版本维护。只有配置结构、字段、默认值或配置语义发生变化时，才修改配置版本；发生变化时，配置版本必须直接同步到承载该变更的项目版本号，而不是按旧配置版本递增。例如，项目在 `0.1.21` 修改配置时，配置版本同步提升到 `0.1.21`；如果 `0.1.22` 没有修改配置，配置版本仍保持 `0.1.21`。仅修改代码、文档或插件版本时，不得连带修改配置版本。
- 任何会改变插件版本的修改，都必须同步维护根目录下的 `CHANGELOG.md`，写明新增、变更、修复、弃用或移除的内容。Changelog 应遵循主流开发规范，保留 `Unreleased` 小节，并按版本号和发布日期组织历史记录。
- 发布插件版本时，必须在对应发布 commit 上创建带注释的 `v<版本号>` Git tag，并单独推送该 tag；`git push origin v<版本号>` 不得隐含或替代分支推送。推送任何分支前必须取得用户明确授权。

## 函数文档字符串

- 所有函数和方法，包括公开接口、私有辅助方法、生命周期方法、构造方法、属性方法以及测试辅助函数，都必须编写 Google 风格文档字符串。
- 文档字符串首先用一段完整描述说明函数的职责、关键行为和重要副作用，不能只重复函数名。涉及缓存、持久化、后台任务、网络调用或状态推进时，应说明操作顺序及失败时的行为。
- `Args` 小节必须逐项说明除 `self`、`cls` 外的全部参数，包括参数语义、允许的格式、特殊值和默认值的影响。`*args`、`**kwargs` 应说明实际支持或读取的内容。
- 函数存在有意义的返回值时必须提供 `Returns` 小节，说明返回类型、结构、关键字段及成功或失败语义。返回 `None` 时，可以省略 `Returns`，但必须在职责描述中说明其状态变化或副作用。
- 函数会主动抛出或允许调用方处理特定异常时，应提供 `Raises` 小节并说明触发条件；不要记录已经在函数内部捕获的异常，也不要虚构实际上不会抛出的异常。
- 当输入格式、返回结构、状态转换或调用方式不直观时，应增加 `Examples` 小节，给出简短且可执行思路清晰的输入输出示例。简单访问器或语义已经完全明确的方法不必机械添加示例。
- 文档字符串与用户可见文本优先使用简体中文；SDK 固有字段名、类型名和协议名称保持原文。代码行为变化时必须同步更新相关文档字符串，禁止保留已经失效的参数、返回值或异常说明。
- 测试函数的文档字符串除测试场景外，还应说明期望结果以及所防止的回归；使用 pytest fixture 时，应在 `Args` 中说明 fixture 的用途。

示例：

```python
def load_model(path: str, device: str = "cpu") -> Model:
    """从指定检查点加载模型，并将其放置到目标计算设备。

    Args:
        path: 模型检查点文件路径。
        device: 承载模型的计算设备，例如 ``"cpu"`` 或 ``"cuda:0"``。

    Returns:
        已加载并移动到目标设备的模型实例。

    Raises:
        FileNotFoundError: 当 ``path`` 指向的检查点不存在时抛出。
    """
```

# 插件功能

本插件用于让 MaiBot 像人类在群聊中分享有趣内容一样，自主判断是否把一个群中的合并转发消息分享至其他群聊。

完整处理由源群 Planner、插件和目标群 Planner 分工完成：

1. 允许作为 source 的群收到合并转发消息后，MaiBot Planner 必须先调用 `view_forward_message` 查看完整内容，再决定是否调用本插件 `@Tool` 请求转发（路径 A），并显式传入源消息 `msg_id`。
2. `maisaka.planner.after_response` 的 EARLY blocking Hook 必须无 I/O 地清洗整批转发调用，只保留公开参数；同一 session 的每个新响应轮先撤销旧凭据，再签发绑定 Host 提供的真实 Planner session 与 `msg_id` 的一次性内部凭据。LATE Hook 只能验证该绑定，不能重新签发或改绑。模型提供的 `platform`、`group_id`、`stream_id`、`chat_id`、target 或内部凭据均不可信。
3. 路径 A 复用当前 Planner 上下文中已经成功取得的完整 ToolResult，不重复查看。路径 B 仅作为 Planner 偶然漏看路径 A 时的 fallback：转发 Tool 单独出现时，由 LATE `after_response` Hook 在发送前改写成真实 `view_forward_message`；后续 `before_request` 必须精确匹配系统调用 ID 与 `msg_id`，成功或达到既有 fallback 边界后才恢复原转发请求。Planner 可见的 Tool 描述不得把路径 B 作为常规选择。
4. 路径 B 的可重试故障和首次空内容按既有阈值续轮重新查看；参数、消息类型、终止或无法安全分类的失败由正式处理器拒绝。混合多工具批次不得插入、删除或重排，只进行清洗和凭据签发；未查看的转发调用由正式处理器拒绝。
5. 正式 Tool handler 必须消费一次性凭据，只使用其中绑定的真实 session 解析可信平台和 source 群号，再校验 source 白名单、消息归属、消息类型和永久防重，然后读取原始转发节点。凭据缺失、未知、错配、已消费，或可信 session 无法解析时必须拒绝，不得回退信任模型上下文字段。
6. 插件按照配置顺序，将消息依次发送到允许作为 target 的群；初版不使用 LLM 选择目标群，也不提供黑名单。每个目标群发送成功后按配置决定是否强制触发目标群 Planner，不重复注入 source 群完整内容。
7. 两条 source 路径共用同一 target 投递链。目标群 Planner 在当前 Host 的上下文窗口内可以看到本群近期聊天；热运行时通常只显示真实合并转发的消息前缀和目标 ID，冷启动最多显示前四个节点预览。Planner 可按目标消息自身展示的 `msg_id` 再调用 `view_forward_message`；但插件不强制这次目标侧查看，也尚不能把 source 完整内容与目标真实消息建立结构化关联。Host 未向插件返回最终 ID 或 Planner 无法可靠定位时必须保持沉默，不得宣称目标 Planner 已能准确回复。

源群只能来自 source 白名单，目标群只能来自 target 白名单。一个任务只有一个 source 消息，但可以有多个 target；各 target 按配置顺序发送和触发，不并行发起投递。当前 SDK 无法等待某个目标群 Planner 完整执行结束，因此初版只保证发送和主动任务入队的顺序，不保证不同目标群的 Planner 推理严格串行。

路径 B 的 pending 与未消费凭据只保存在当前插件进程；精确 ToolResult 缺失、配置更新或插件卸载时必须清除，不能在未来普通轮次恢复旧请求。LATE 编排 Hook 超时或失败时，EARLY 清洗与凭据边界仍然有效，原调用只能安全降级到正式处理器拒绝并要求查看，不能直接发送。

插件应优先复用 source 侧 `view_forward_message` 已经得到的完整内容，避免重复查看或重复进行媒体分析。原始消息节点用于实际发送；完整文本或 fallback 当前仅用于 source 分享资格，不写入目标群合成上下文。将完整内容与目标真实消息 ID 建立幂等关联仍依赖 MaiBot Host 上游能力；实现前不得恢复 `maisaka.context.append` 重复注入真实消息、转发节点或媒体二进制。

详细的功能边界、处理流程、缓存方案、失败处理和测试要求见 [功能设计文档](docs/forward-message-design.md)。
